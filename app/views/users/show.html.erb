<h1>Welcome, <%= @user.email%>!</h1>
<div class="row">
  <div class="col-md-8">
    <div class="media">
      <% @topics.each do |topic| %>
      <h2><%= topic.title %></h2>
         <%= render topic.bookmarks %>
      <% end %>
    </div>
  </div>
</div>
